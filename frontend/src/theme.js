import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: "'Balsamiq Sans', system-ui, sans-serif",
  },
  palette: {
    primary: {
      main: '#946846',
    },
  },
});

export default theme;

